<!--index.wxml-->
<view class="container">
    <!-- 用户登录测试 -->
    <!--<view class="userinfo" bindlongpress="toDataPage">
        <image
            class="userinfo-avatar"
            src="{{logged ? userInfo.avatarUrl : './user-unlogin.png'}}"
            background-size="cover"
        ></image>
        <view>
            <text class="userinfo-nickname">{{logged ? userInfo.nickName : '点击登录'}}</text>
            <text class="userinfo-nickname" wx:if="{{logged}}">{{userInfo.lastLoginTime}}</text>
        </view>
    </view>
    -->

    <!-- 信道连接测试 -->
    <view class="uploader-image" bindlongpress="toDataPage">
        <image mode="scaleToFill" src="{{imageSrc}}" >
        </image>
    </view>
    <view class="uploader">
        <view wx:if="{{deviceCode == ''}}">
          <button type="primary" bindtap='doSendRequest'>{{logged ? '扫码出纸' : '授权登录'}}</button>
        </view>
        <view wx:else>
          <button type="primary" bindtap="doSendRequest" >{{logged ? '立即出纸' : '授权登录'}}</button>
        </view>        
    </view>
</view>
